<project name="Eternally Social" default="build">
	<target name="clean">
		<delete dir="${basedir}/build" />
	</target>

	<target name="prepare">
		<mkdir dir="${basedir}/build" />
		<mkdir dir="${basedir}/build/logs" />
		<mkdir dir="${basedir}/build/coverage" />
		<mkdir dir="${basedir}/build/codebrowser" />
                <mkdir dir="${basedir}/build/cpd" />
                <mkdir dir="${basedir}/build/dependencies" />
                <mkdir dir="${basedir}/build/pmd" />
                
	</target>

	<target name="lint">
		<exec dir="${basedir}" executable="php" failonerror="true">
			<arg line="-l" />
		</exec>
	</target>

	<target name="phpcs">
        <exec dir="${basedir}" executable="phpcs" failonerror="false" output="/dev/null">
            <arg line="--extensions=php --ignore=build/*,tests/*,library/Zend/* --standard=Zend --tab-width=4 --report=checkstyle --report-file=${basedir}/build/logs/checkstyle.xml ." />
        </exec>
    </target>
 
    <target name="phpmd">
        <exec dir="${basedir}" executable="phpmd" failonerror="false">
            <arg line=". xml codesize --exclude=build,tests,library/Zend --reportfile ${basedir}/build/logs/pmd.xml" />
        </exec>
        <exec dir="${basedir}" executable="xsltproc" failonerror="false" output="${basedir}/build/pmd/index.html">
            <arg line="/root/pmd-report.xslt ${basedir}/build/logs/pmd.xml" />
        </exec>
    </target>
 
    <target name="phpcpd">
        <exec dir="${basedir}" executable="phpcpd" failonerror="false">
            <arg line="--log-pmd ${basedir}/build/logs/pmd-cpd.xml --exclude=build --exclude=tests --exclude=library/Zend --suffixes php ." />
        </exec>
        <exec dir="${basedir}/build/logs" executable="xsltproc" failonerror="false" output="${basedir}/build/cpd/index.html">
            <arg line="/root/cpdhtml.xslt ${basedir}/build/logs/pmd-cpd.xml" />
        </exec>
    </target>
 
    <target name="pdepend">
        <exec dir="${basedir}" executable="pdepend" failonerror="false">
            <arg line="--jdepend-xml=${basedir}/build/logs/jdepend.xml
                       --jdepend-chart=${basedir}/build/dependencies/jdepend.svg
                       --overview-pyramid=${basedir}/build/dependencies/pyramid.svg
                       --optimization=best
                       --exclude=build,tests,library/Zend
                       ." />
        </exec>
        
    </target>
 
    <target name="phpcb">
        <exec dir="${basedir}" executable="phpcb" failonerror="false">
            <arg line="--log ${basedir}/build/logs --source ${basedir} -e ${basedir}/build -e ${basedir}/library/Zend --output ${basedir}/build/codebrowser" />
        </exec>
    </target>
 
  
 
   
 
    <target name="build" depends="clean,prepare,lint,phpcs,phpmd,phpcpd,pdepend,phpcb" />
</project>
